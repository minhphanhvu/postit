$total_votes = $('#<%= @comment.class.to_s.downcase %>_<%= @comment.id %>_votes');
$total_votes.text(<%= @comment.total_votes %>);
$upvote = $total_votes.siblings('.upvote');
$downvote = $total_votes.siblings('.downvote');

if (<%= @upvote %>) {
  $upvote.html('<i><i class="fa fa-arrow-up fa-lg disabled"></i>');
  $downvote.html('<%= link_to(vote_post_comment_path(@comment.post, @comment, vote: false), method: 'post', remote: true) {|x| '<i class="fa fa-arrow-down fa-lg"></i>'.html_safe } %>');
} else {
  $downvote.html('<i><i class="fa fa-arrow-down fa-lg disabled"></i>');
  $upvote.html('<%= link_to(vote_post_comment_path(@comment.post, @comment, vote: true), method: 'post', remote: true) {|x| '<i class="fa fa-arrow-up fa-lg"></i>'.html_safe } %>');
}